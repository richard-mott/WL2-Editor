<UserControl x:Class="WL2.Editor.Views.CharacterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:assisticant="clr-namespace:Assisticant;assembly=Assisticant.XAML"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            
        </Grid.ColumnDefinitions>
        
        <TextBlock Grid.Row="0" Text="{Binding Path=Name, StringFormat=Name: {0}}" />
        
        <ItemsControl Grid.Row="1" ItemsSource="{Binding Attributes}">
            <ItemsControl.ItemTemplateSelector>
                <assisticant:ViewSelector />
            </ItemsControl.ItemTemplateSelector>
        </ItemsControl>
    </Grid>
</UserControl>
